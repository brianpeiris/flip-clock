{
  "private": true,
  "scripts": {
    "dev": "esbuild --sourcemap --bundle --outfile=bundle.js --watch index.js",
    "build": "esbuild --minify --sourcemap --bundle --outfile=bundle.js index.js"
  },
  "dependencies": { "esbuild": "^0.12.24", "gsap": "^3.7.1", "holoplay": "^1.0.3", "three": "^0.132.2" }
}
